#pragma mode( separator(.,;) integer(h64) assignment(=) indentation(2) )
#require "GROB"

#include <spectrum>

export START()
begin
	RECT();
	IMAGE(G0, 0, 0, spectrum, 20, 240);
	WAIT(-1);
	
    return 0;
end;

export RESET()
begin
	return 0;
end;
