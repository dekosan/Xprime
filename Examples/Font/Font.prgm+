#pragma mode( separator(.,;) integer(h64) )
#require <HP>
#include "CGA.h"

#define WHITE #FFFFFF:32h
#define BLACK 0
export START()
begin
	RECT(BLACK);
	HP.TEXT(G0, "CGA Font Example", 0, 0, CGA, WHITE);
	WAIT(-1);
    return 0;
end;

export RESET()
begin
    return 0;
end;