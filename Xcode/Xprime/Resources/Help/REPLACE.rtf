
{\rtf1\ansi\deff0
{Syntax:}\par REPLACE(object,start,object)\par
\par Replaces portion of a matrix, vector or string starting at start by object.\par For a matrix, start is a list containing two numbers; for a vector or string it is a single number.\par Note: for strings, you can do: REPLACE("string", "sub_string", "replace_string")\par
}