
{\rtf1\ansi\deff0
{Syntax:}\par IF test THEN command(s) [ELSE commands] END;\par
\par Evaluates test. If test is true (non 0), executes command(s); otherwise, executes the comands in the ELSE clause nothing happens.\par \par IF A<1 \par THEN PRINT("A IS SMALLER THAN 1"); \par ELSE PRINT("A IS LARGER THAN 1"); \par END;\par
}