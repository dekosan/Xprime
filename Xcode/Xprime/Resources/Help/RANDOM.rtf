
{\rtf1\ansi\deff0
{\par \b Syntax:}\par RANDOM([a],[b],[c])\par
{\par \b Description:}\par Random number. Returns a pseudo-random number generated using a seed value, and updates the seed value.\par \par With no argument, this function returns a random number x with 0 \u8804? x < 1. With one argument, this returns a random number x with 0 \u8804? x < a. With two arguments, this returns a random number x with a \u8804? x < b. With three arguments, this returns a list of size a with each element being a random number x with b \u8804? x < c.\par
{\par \b Example:}\par RANDOM(3,0,10) returns \{ random1, random2, random3 \}\par }